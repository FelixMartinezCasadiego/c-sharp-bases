partial class Program
{
    static void DataTypes()
    {
        int integer = 42; // Integer number
        double floatingPoint = 3.14; // Double-precision floating-point number
        bool isTrue = true; // Boolean value
        char character = 'A'; // Single character, must use single quotes
        string text = "Hello, World!"; // Sequence of characters

        Console.WriteLine($"Integer: {integer}, Double: {floatingPoint}, Boolean: {isTrue}, Char: {character}, String: {text}");
    }
}
